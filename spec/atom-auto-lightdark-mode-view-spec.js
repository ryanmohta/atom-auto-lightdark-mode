'use babel';

import AtomAutoLightdarkModeView from '../lib/atom-auto-lightdark-mode-view';

describe('AtomAutoLightdarkModeView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
